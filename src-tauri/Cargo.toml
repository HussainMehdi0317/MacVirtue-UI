[package]
name = "macvirtue-ui"
version = "0.1.0"
authors = ["Your Name"]
edition = "2021"

[dependencies]
tauri = { version = "2.0", features = ["shell", "dialog"] }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
dirs = "5.0"

[build-dependencies]
tauri-build = { version = "2.0", features = [] }
